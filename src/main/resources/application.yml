server:
  port: 8080
logging:
  level:
    org:
      springframework:
        web: DEBUG
spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}

swagger:
  enabled: true

service:
  url:
    ms-auth: http://localhost:8081
#https://gdg-ms-auth.herokuapp.com


